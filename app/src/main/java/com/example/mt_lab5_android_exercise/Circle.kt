package com.example.mt_lab5_android_exercise

class Circle(verticesCount: Int, radius: Float) {
    private val verticesCount: Int
    private val radius: Float

    init {
        this.radius = radius
        this.verticesCount = verticesCount
    }
}